# Build output
dist/
node_modules/

# Recordings / capture artifacts (never commit)
*.mp4
*.cast

# MCP config (contains local paths / secrets — copy from .mcp.example.json)
.mcp.json

# Environment (credentials — NEVER commit)
.env
.env.*
!.env.example

# macOS
.DS_Store

# MetaMCP data directory
.metamcp/

# Mentu build artifacts
.mentu/

# Internal docs
docs/

# Docs site (deployed separately)
docs-site/
